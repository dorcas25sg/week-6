/* ================================
Week 6 Assignment: Midterm Functions + Signatures
================================ */


var myStyle // if it's a hotspot, color red; if it's a breeding ground, color it blue; if it's a dengue case, color it green

var filterPolygons //only show points with respect to the slide. e.g. If it's the slide on hotspots, only hotspot polygons are shown. Reads property: name

var resetMap // map is set to default zoom and coordinates

var highProfilePages // automatically pops up and zooms in when slideFive and slideSix are launched
